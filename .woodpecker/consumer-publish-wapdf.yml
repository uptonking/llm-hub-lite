# generated by ops/generate-woodpecker-workflows.sh; do not edit
skip_clone: true
when:
  - event: push
    branch: main
    path:
      include:
        - apps/wapdf/**
        - config/cluster/apps/wapdf.policy
        - config/cluster/overrides/**/wapdf.env
        - apps/wapdf/route*.caddy
        - apps/wapdf/images.lock.env
      on_empty: false

depends_on:
  - consumer-stage-wapdf-worker-2
  - control-sync-leader
  - name: foundation-reconcile-leader
    optional: true
  - name: cluster-reconcile-leader
    optional: true

labels:
  node: leader
  deployment: "true"
  target: production
  repo: uptonking/llm-hub-lite

concurrency:
  limit: 1
  group: llm-hub-lite-deployment

steps:
  publish:
    image: llm-hub-lite/deploy-runner:current
    pull: false
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/apps/llm-hub-lite:/opt/apps/llm-hub-lite
      - /opt/platform:/opt/platform
      - /opt/backups/llm-hub-lite:/opt/backups/llm-hub-lite
      - /run/lock/llm-hub-lite:/run/lock/llm-hub-lite
      - /etc/llm-hub-lite:/etc/llm-hub-lite
      - /usr/local/bin/platform-submit:/usr/local/bin/platform-submit:ro
      - /usr/local/bin/platformctl:/usr/local/bin/platformctl:ro
      - /usr/local/bin/configure-app-secrets:/usr/local/bin/configure-app-secrets:ro
    commands:
      - DEPLOY_DEBUG_LEVEL=off CONSUMER_APP_ID=wapdf /usr/local/bin/platform-submit consumer-publish "$CI_COMMIT_SHA"
